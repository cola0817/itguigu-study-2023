<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="itguigu.study.mapper.UserMapper">


    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user
        (username,password)
        VALUES
        (#{userName},#{passWord})
    </insert>

    <update id="updateUser">
        UPDATE user SET password=#{passWord} WHERE id = #{userId}
    </update>

    <delete id="deleteUser">
        DELETE FROM user WHERE id = #{userId}
    </delete>

    <select id="selectByUserId" resultType="itguigu.study.entity.User">
        SELECT id,username,password FROM user WHERE id = #{userId}
    </select>

    <select id="selectAll" resultType="itguigu.study.entity.User">
        SELECT id,username,password FROM user
    </select>


</mapper>